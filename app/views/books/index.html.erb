<h1>Books</h1>
<h2>Hi, I'm a list of books!</h2>

<ul>
	<% @books.each do |book| %>
	<li><%= link_to book.title, book %> by <%= book.author %>. <%= link_to 'Edit', edit_book_path(book) %></li>
	<% end %>
</ul>